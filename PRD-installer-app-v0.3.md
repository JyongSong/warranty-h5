# PRD：设置师傅端（Installer App）+ A/S 管理 v0.3（需求基本定稿）

> 状态：需求侧基本定稿。唯一待补：증상코드 分类表（你之后给，不阻塞）。
> 落地前会经 spec-panel 评审 + workflow 拆实现计划。

---

## 1. 背景与目标

现在的 backoffice 只给**总部**用。本项目新增**安装师傅交互端（打包成安卓 App）**，把已开发完成但搁置的**新派单系统正式上线**，并新增**A/S（售后维修）管理**。

师傅端要能：
1. 看到派给自己的**安装单**和**A/S 单**
2. 接受 / 拒绝
3. 完成后登记结果 + 上传照片（安装单）/ 填写处理详情 + 服务费（A/S 单）

总部据此**审核 + 结算**，替代目前"导出 Excel + 人工发短信"的老流程。

---

## 2. 范围

### In scope
- 师傅端：登录、我的工单（安装 + A/S）、接受/拒绝、完成登记、照片上传
- **安卓 App 打包**：Web 内核（现有 Next.js）+ Capacitor + FCM 推送 + 上架 Google Play
- 新派单系统正式上线（配置 + 数据，非功能开发）
- **A/S 管理**：管理员建单、按安装记录派单、师傅处理、服务费审批
- 总部侧：完成复核、照片查看、**完整结算**（含安装费 + A/S 服务费）
- 对象存储（Supabase Storage）

### Out of scope
- **纯原生开发（Kotlin/Java）**——采用 web 内核 + Capacitor 打包，不写原生 UI
- 老派单 `send-assignment-sms` Excel 链路改造（新派单上线后另行退役）
- 客户侧 `/i/c` 流程改动
- iOS App（本期只做安卓；web 内核未来可复用）

---

## 3. 关键前提：新派单系统上线

新派单自动化主线（ERP同步 → 客户填地址 → 匹配师傅 → **总部审核** → 通知师傅 → 接受/拒绝）**已完整实现且测试齐全，只是被开关关闭**（搁置原因＝"做完没启用"）。上线需：

1. 翻开数据库开关：`sync-orders.enabled`、`dispatcher.enabled`、`sms.deliveryMode`(→production)、`customerInputRequestMode`
2. 配环境变量：ERP、Solapi、PII 加密 key、cron secret、SMS link base URL
3. **录入真实师傅数据**（服务区域、能力、`aqaraAppCapability`、电话），否则匹配走空
4. 保留总部审核（AUTO 派单先落 `WAITING_ADMIN_REVIEW`，通过后才通知师傅）✅

---

## 4. 角色

| 角色 | 说明 |
|---|---|
| 师傅（Installer） | 新增使用者；身份 = `installers` 表（手机号唯一） |
| 总部管理员（HQ Admin） | 现有 backoffice 用户（`BackofficeUser`，level≥1） |

---

## 5. 平台（已定）

**Web 内核 + Capacitor 打包成安卓 App。**
- 一套现有 Next.js 代码，用 Capacitor 打包为安卓 App，上架 Google Play
- 原生相机（拍照上传）+ **FCM 推送**（新单/新 A/S 提醒）
- 不写原生 UI（Kotlin/Java）；开发量接近纯 H5
- 代价：一次 Google Play 开发者账号（$25）+ 打包/上架配置
- 只做安卓；web 内核将来可复用到 iOS/纯网页

---

## 6. 师傅端 · 认证 + 工单

### 6.1 认证（已定）
- 手机号做账号（沿用 `installers.phone`）
- 首次/换机/忘密码：短信 OTP；验证后可设密码，日常用"手机号 + 密码"快速登录
- 仅 `installers` 表中存在且启用的号码可登录
- 师傅端建立**独立会话机制**（现有 backoffice 走 Supabase，师傅端另建）🟡 实现细节评审阶段定

### 6.2 我的工单列表
- 登录后看到派给本师傅的**两类工单**：安装单、A/S 单
- 按状态分组：待响应 / 待安装（已接受）/ 待处理（A/S）/ 已完成·待复核 / 已结算
- 每条显示：类型、日期时段、地址、产品/症状摘要
- 客户姓名/电话在**接受前隐藏**（沿用 `/i/i` PII 策略）

### 6.3 接受/拒绝（安装单）
- 复用现有 `respondInstallerAssignment` + 状态机
- 接受 → `INSTALLER_ASSIGNED`（触发客户确认 + happycall 短信）
- 拒绝 → 必选理由 → 自动转派下一候选（最多 2 次 AUTO）
- 超时（默认 24h）→ 自动转派/异常
- ⚠️ 从"一次性 token 链接"改为"登录后在列表操作"，把 assignment 接到师傅账号下（一师傅 = 多工单）

### 6.4 安装完成登记（核心）
师傅点"完成"填写：

**A. 安装结果选项**
| 选项 | 取值 | 计费 | 照片 |
|---|---|---|---|
| 월패드 연동 | 是/否 | ❌ 0원 仅记录（现场结清） | 无 |
| 联动等级（**递进单选**） | 无 / APP / 허브 | APP=+费率；허브=+费率（含APP） | 选 APP 或 허브 → **强制传 APP 界面照片** |

- 复用现有枚举 `aqaraAppCapability`；一张 APP 界面照即可判断实际是 APP 还是网关
- **★ 安装结束 日期/时间（强制）** —— 夜间/周末费判定依据

**B. 照片**：至少 1 张、最多 4 张；有联动则必须含那张 APP 界面照；存 Supabase Storage

**C. 要求 vs 实际 自动对照**：派单已推断"要求联动等级"(`required_aqara_app_capability`)，与实际值对照，实际<要求 → 自动标异常供复核

---

## 7. A/S 管理（新模块）

> 现状：`backoffice/as/register`、`as/search` 为**空壳占位页**，另有 `as/layout.tsx.bak`（曾实现后回退）。基本从零建。

流程：**管理员在「A/S 등록」建单 → 派给师傅 → 师傅接受后出动 → 处理完成填详情 + 服务费（管理员审批）**。

### 7.1 A/S 등록（管理员建单）
字段参考现有 ERP「A/S수리입력」（UI 不必一致）：
- 出库日期、网点名、仓库、负责人、维修类型、客户名、联系电话、订单号、地址、安装完成与否、安装日期、服务费、负责师傅、备注
- **新增**：`증상코드`（症状分类代码，🟡 分类表你之后给）、`상세 증상`（详细症状描述）

### 7.2 派单（已定）
- **优先按 backoffice 历史安装**：输入订单号 / 用户电话 / SN → 查 backoffice 安装记录（`installation_orders` 有 `source_erp_order_no` + `currentInstaller`）→ 自动带出当初安装的师傅（"谁装的谁修"）
- **查不到** → **管理员按地址手动分配**；系统复用现有地区匹配 `matcher` 给出**推荐候选师傅**，不用纯手填
- 说明：backoffice 历史依赖新派单上线后逐步积累，上线初期多走"按地址分配"；新派单上线前的老安装是否导入关联 = 实现细节🟡

### 7.3 师傅 A/S 单页（已定）
- 显示必要信息：客户、地址、联系方式、订单/安装信息、`증상코드` + `상세 증상`
- **A/S 单也走接受/拒绝**（与安装单一致）

### 7.4 A/S 完成（已定）
- 师傅填**处理详情**（文字）
- 师傅填 **용역비（服务费）→ 管理员事后审批**（"申报-审核"模式）
- 照片：**可选、非强制**上传
- 审批通过的 용역비 **计入结算**（见 §8.4）✅

---

## 8. 总部侧（backoffice）

### 8.1 派单审核（保留，已定）
沿用 `WAITING_ADMIN_REVIEW` → 通过 → 通知师傅。

### 8.2 完成复核（已定：要）
师傅提交安装完成后进入**"待复核"**，总部核对照片/选项 → 确认 → 才最终完成并进结算。

### 8.3 照片查看
总部在单详情查看师傅上传照片（含 APP 界面证明照、A/S 照片若有）。

### 8.4 结算（完整流程，已定）

**结算金额构成**

*安装单*
| 项 | 来源 |
|---|---|
| 联动费（APP / 허브） | 师傅选 option → 自动，有照片 |
| 출장비（出差费） | 系统自动固定加 |
| 장거리 이동（远途） | 师傅填金额 → 管理员审核 |
| 야간/주말 | 据师傅填的"安装结束时间"自动判定 → 加 |
| 월패드 | 0，仅记录 |

*A/S 单*
| 项 | 来源 |
|---|---|
| 용역비（服务费） | 师傅填 → 管理员审批 |

**费率机制（已定）**：**全局默认 + 师傅级覆盖**
- 系统设置配一套全局默认费率；每个师傅档案可单独覆盖任意项
- 结算时该师傅有覆盖用覆盖，否则用默认；金额一律走系统设置可配置

**流程（已定）**：自定义周期 · 待结算/已结算两态 · 管理员操作 · 导出 Excel · 按师傅/周期汇总 · **师傅端可看自己明细**

---

## 9. 数据模型改动（初步，评审阶段细化）

**复用/扩展**：`InstallationOrder` / `InstallationInstallerAssignmentAttempt` / `Installer` / `InstallationOrderStatusEvent`；状态机已允许 `INSTALLER_ASSIGNED → COMPLETED`（新增 `actorType:"INSTALLER"` 完成动作 + "待复核"态）；`aqaraAppCapability` 复用为实际联动等级。

**新增**：
1. 师傅账号/会话（密码 hash + session）
2. 师傅费率覆盖（挂 `Installer` 或独立表）+ 全局默认费率（系统设置）
3. 安装完成凭证表（월패드、实际联动等级、安装结束时间、照片 URL 列表、提交人/时间、复核状态）
4. **A/S 工单表**（安装记录关联、症状码、详细症状、派单师傅、接受/拒绝、处理详情、용역비、审批状态、照片）
5. 结算表（周期、状态、明细、金额）
6. 对象存储：Supabase Storage bucket + 按师傅隔离的访问权限

---

## 10. 非功能
- **PII**：客户姓名/电话沿用加密存储 + 接受前隐藏
- **权限**：师傅只看/操作自己的工单；照片按师傅隔离
- **通知（已定）**：新单/新 A/S 用 **FCM 推送** + 短信（现有模板）兜底
- **审计**：完成/复核/审批/结算动作落 `InstallationOrderStatusEvent`（或对应审计）

---

## 11. 分期（已认可）

| 阶段 | 内容 |
|---|---|
| **P0 上线地基** | 翻开关 + 配环境 + 录师傅数据，新派单真实跑（不依赖 App，可先行） |
| **P1 师傅端 MVP** | Capacitor 打包脚手架 + 认证 + 我的工单 + 安装单接受/拒绝 + FCM 推送 |
| **P2 安装完成登记** | option + 照片 + 结束时间 + 要求/实际对照 + 总部照片查看/复核 |
| **P3 A/S 管理** | 建单 + 按安装记录/地址派单 + 师傅接受-处理 + 용역비申报 |
| **P4 结算** | 完整结算（安装费 + A/S 费 + 费率默认/覆盖 + 导出） |

---

## 12. Open Questions（剩余）

1. 🟡 **증상코드 分类表**（你之后给）
2. 🟡 师傅端会话机制实现细节（评审阶段定）
3. 🟡 新派单上线前的老安装记录是否导入，用于 A/S 追溯（§7.2）
