# PRD：设置师傅端（Installer App）+ A/S 管理 v0.4

> 状态：需求定稿 + spec-panel 评审回填。开工前须先闭环 §0 的前置验证。
> 待补：증상코드 分类表（你之后给，不阻塞）。

## v0.4 变更摘要（基于 spec-panel 评审）
- 新增 **§0 开工前置验证**（评审 C1 打包可行性 / C2 A/S 历史数据源）
- 新增 **§6.5 师傅会话授权与超时判定**（C3）
- 新增 **§8.5 结算金额快照与费率生效**（M1）
- 补齐异常流：完成复核**驳回流**（M2）、A/S **拒绝退回**（M3）、照片**弱网/失败/离线**（M5）
- 补规则：实际>要求的**计费权威**（M4）、월패드现场金额留痕（m1）、A/S SLA（m2）、新老派单**防双重派单**（m3）
- 安全补强：OTP 现有缺陷需重做（M6）、照片存储 signed-URL/RLS（M7）、师傅只见自己费率（m4）
- 数据模型补：费率快照、FCM device token（m5）、会话/OTP 持久化
- P0 补隐藏工作量：师傅结构化数据录入路径（M8）

---

## 0. 开工前置验证（必须先闭环，否则 P1+ 会返工）

| 编号 | 验证项 | 为什么 | 做法 |
|---|---|---|---|
| **C1** | Capacitor 打包 Next.js 可行性 spike | 现有 backoffice 是 SSR + server actions + API routes，**不能直接静态导出**成 app 内嵌资源；可行路径通常是"Capacitor 壳加载线上远程 URL + 原生相机/推送插件"。"开发量接近纯 H5"需验证 | 先做一个空壳 Capacitor：载入线上师傅端 + 拍照 + FCM 各跑通一个，再定架构与工作量 |
| **C2** | A/S 历史安装数据源 | 新派单刚上线、`installation_orders` 历史为空；老安装在 `warranty_registrations`（无订单号）或 ERP。不解决则 A/S "谁装谁修"初期落空 | 决定是否导入历史安装记录，或明确接受"初期 A/S 靠人工查 ERP"（§7.2） |

---

## 1. 背景与目标

backoffice 现只给**总部**用。本项目新增**安装师傅端（打包成安卓 App）**，把已开发完成但搁置的**新派单系统正式上线**，并新增 **A/S（售后维修）管理**。

师傅端要能：①看到派给自己的安装单 / A/S 单 ②接受/拒绝 ③完成后登记结果 + 上传照片（安装）/ 填处理详情 + 服务费（A/S）。总部据此**审核 + 结算**，替代"导出 Excel + 人工发短信"。

---

## 2. 范围

**In**：师傅端（登录、工单、接受/拒绝、完成登记、照片）；安卓 App 打包（Capacitor + FCM + Google Play）；新派单上线（配置+数据）；A/S 管理；总部侧完成复核/照片查看/**完整结算**；对象存储（Supabase Storage）。

**Out**：纯原生 Kotlin/Java；老派单 Excel 链路改造（另行退役）；客户侧 `/i/c` 改动；iOS App（web 内核未来可复用）。

---

## 3. 关键前提：新派单系统上线（P0）

自动化主线（ERP同步→客户填地址→匹配→**总部审核**→通知师傅→接受/拒绝）**已实现且测试齐全，只是开关关闭**。上线需：

1. 翻开关：`sync-orders.enabled`、`dispatcher.enabled`、`sms.deliveryMode`（**先 test 后 production**）、`customerInputRequestMode`
2. 配 env：ERP、Solapi、PII 加密 key、cron secret、SMS link base URL
3. **录入真实师傅数据**（服务区域、能力、`aqaraAppCapability`、电话）
   - ⚠️（评审 M8）现有 CSV 导入映射**不含结构化字段**（只有 name/phone/branch/region/coverage/address/category）。结构化能力/服务区/APP能力可走 **`/survey` 邀请函**收集（该表单已采集这些字段并写入 `installers`）——即"发邀请函录师傅"这条路正好服务 P0
4. 保留总部审核（AUTO 先落 `WAITING_ADMIN_REVIEW`）✅

---

## 4. 角色

| 角色 | 说明 |
|---|---|
| 师傅（Installer） | 身份 = `installers` 表（手机号唯一） |
| 总部管理员（HQ Admin） | 现有 `BackofficeUser`（level≥1） |

---

## 5. 平台（已定，架构待 C1 验证）

**Web 内核（现有 Next.js）+ Capacitor 打包安卓 App**：上架 Google Play、原生相机、FCM 推送、不写原生 UI。代价：Google Play 账号（$25）+ 打包配置。
（评审 C1）具体是"Capacitor 壳载远程 URL"还是"静态导出子应用"，**待 §0 spike 定**。只做安卓。

---

## 6. 师傅端 · 认证 + 工单

### 6.1 认证（已定）
- 手机号做账号；首次/换机/忘密码走短信 OTP；验证后可设密码，日常"手机号+密码"登录
- 仅 `installers` 表中存在且启用的号码可登录
- （评审 M6）**不直接复用现有 `api/auth/sms` 的 OTP**：现有实现是内存存储（重启/多实例失效）+ 明文打日志。师傅端 OTP 须**持久化 + 限流防爆破 + 不记明文**

### 6.2 我的工单列表
- 两类工单（安装 / A/S），按状态分组：待响应 / 待安装 / 待处理(A/S) / 已完成·待复核 / 已结算
- 每条显示类型、日期时段、地址、产品/症状摘要
- 客户姓名/电话**接受前隐藏**

### 6.3 接受/拒绝（安装单）
- 复用 `respondInstallerAssignment` + 状态机
- 接受→`INSTALLER_ASSIGNED`（触发客户确认 + happycall 短信）；拒绝→必选理由→自动转派下一候选（最多 2 次 AUTO）；超时→转派/异常

### 6.4 安装完成登记（核心）
师傅点"完成"填写：

**A. 结果选项**
| 选项 | 取值 | 计费 | 照片 |
|---|---|---|---|
| 월패드 연동 | 是/否（+现场金额，见下） | 现场结清，总部仅**留痕**（评审 m1） | 无 |
| 联动等级（递进单选） | 无 / APP / 허브 | APP=+费率；허브=+费率（含APP） | 选 APP/허브 → **强制传 APP 界面照** |

- 复用 `aqaraAppCapability`；一张 APP 界面照即可判断 APP 还是网关
- **★ 安装结束日期/时间（强制）**——夜间/周末费判定依据
- （评审 m1）월패드现场收款金额建议**记录数值**（对账/税务），不只布尔 🟡

**B. 照片**：1–4 张；有联动必须含 APP 界面照；存 Supabase Storage
- （评审 M5）**弱网/失败/离线处理**：单张大小/格式限制；上传失败自动重试；**离线暂存 → 联网补传**（App 核心价值）；全部上传成功才允许提交"完成"

**C. 要求 vs 实际 对照**（评审 M4 计费权威）
- 派单已推断"要求联动等级"(`required_aqara_app_capability`)
- 实际 **<** 要求 → 自动标异常供复核
- 实际 **>** 要求（多装）→ 🟡 **按谁计费需定**：默认建议"按实际、但需总部复核确认"，避免师傅单方加钱
- **金额权威来源 = 复核通过时冻结的快照**（见 §8.5）

### 6.5 会话授权与超时判定（新增，评审 C3）
- 现有接受/拒绝是 **24h 过期、单次 token**；改"登录后列表操作"后，授权改为 **session → installerId → 该师傅名下 assignment**；`respondInstallerAssignment` 增加"按 installerId 校验归属"的授权路径（保留 token 路径兼容旧短信链接）
- **超时判定**：仍以"**通知送达时间 + 24h**"为准（与是否登录无关），到点未响应照旧自动转派——避免师傅拖延登录卡住派单
- 会话有效期、多设备登录策略 🟡 实现阶段定

---

## 7. A/S 管理（新模块）

> 现状：`backoffice/as/register`、`as/search` 为空壳占位页，有 `as/layout.tsx.bak`。基本从零建。

流程：**管理员建单 → 派师傅 → 师傅接受后出动 → 完成填详情 + 服务费（管理员审批）**。

### 7.1 A/S 등록（管理员建单）
字段参考 ERP「A/S수리입력」：出库日期、网点、仓库、负责人、维修类型、客户名、电话、订单号、地址、安装完成与否、安装日期、服务费、负责师傅、备注。
**新增**：`증상코드`（🟡 分类表待给；🟡 是否影响派单/费用待定）、`상세 증상`。

### 7.2 派单（已定，前提见 §0 C2）
- **优先按 backoffice 历史安装**：订单号/电话/SN → 查 `installation_orders`（`source_erp_order_no` + `currentInstaller`）→ 带出原师傅（"谁装谁修"）
- **查不到** → 管理员按地址手动分配；复用 `matcher` 推荐候选
- ⚠️ 上线初期历史为空，多走"按地址"；历史导入见 §0 C2

### 7.3 师傅 A/S 单页（已定）
- 显示客户/地址/联系方式/订单/安装信息 + `증상코드` + `상세 증상`
- **走接受/拒绝**（同安装单）
- （评审 M3）**拒绝后**：A/S 非 matcher 派单，无自动转派 → **退回管理员重新指派**
- （评审 m2）A/S **响应/上门时限（SLA）** 🟡 待定（是否设超时提醒）

### 7.4 A/S 完成（已定）
- 师傅填**处理详情**（文字）+ **용역비 → 管理员事后审批**（申报-审核）
- 照片：**可选、非强制**
- 审批通过的 용역비 **计入结算**（§8.4）✅

---

## 8. 总部侧（backoffice）

### 8.1 派单审核（保留）
`WAITING_ADMIN_REVIEW` → 通过 → 通知师傅。

### 8.2 完成复核（已定：要 + 驳回流，评审 M2）
- 师傅提交安装完成 → **"待复核"**（新增状态/字段，`actorType:"INSTALLER"` 提交）
- 总部核对照片/选项 → **确认** → `COMPLETED` → 进结算
- **驳回**：填驳回理由 → **退回师傅重新登记**（可再次提交）；驳回次数不限但每次留审计
- 状态语义：`INSTALLER_ASSIGNED → (师傅提交) 待复核 → (确认) COMPLETED` / `(驳回) 回到可重填`

### 8.3 照片查看
总部单详情查看师傅上传照片（含 APP 界面证明照、A/S 照片若有）。

### 8.4 结算金额构成（已定）

*安装单*：联动费（option 自动，有照片）+ 출장비（自动固定）+ 장거리（师傅填→管理员审）+ 야간/주말（据安装结束时间自动判）+ 월패드（0 计费，记录现场金额）
*A/S 单*：용역비（师傅填→管理员审批）

**费率机制**：**全局默认 + 师傅级覆盖**（系统设置配默认；师傅档案可覆盖任意项；金额全可配置）
**流程**：自定义周期 · 待结算/已结算两态 · 管理员操作 · 导出 Excel · 按师傅/周期汇总 · 师傅端可看自己明细
（评审 m4）师傅端只能看**自己**的费率与结算，看不到他人

### 8.5 结算金额快照与费率生效（新增，评审 M1）
- **金额在"完成复核通过"时冻结成快照**存入结算明细；结算/汇总只读快照，**不实时按当前费率重算**
- 好处：之后调费率、改 option 不会篡改历史应付
- 费率调整只影响调整后**新冻结**的单；快照记录当时费率来源（默认/师傅覆盖）以备审计
- 管理员审核类费用（远途/용역비）在审批确认时并入快照

---

## 9. 数据模型改动（初步，评审阶段细化）

**复用/扩展**：`InstallationOrder` / `InstallationInstallerAssignmentAttempt` / `Installer` / `InstallationOrderStatusEvent`；状态机加"待复核"态 + `actorType:"INSTALLER"` 完成/驳回；`aqaraAppCapability` 复用为实际联动等级。

**新增**：
1. 师傅账号/会话 + **OTP 持久化表**（含限流计数，评审 M6）
2. 师傅费率覆盖（挂 `Installer` 或独立表）+ 全局默认费率（系统设置）
3. 安装完成凭证表（월패드+现场金额、实际联动等级、安装结束时间、照片 URL、提交人/时间、复核状态/驳回理由）
4. A/S 工单表（安装记录关联、症状码、详细症状、师傅、接受/拒绝、处理详情、용역비、审批状态、照片）
5. 结算表（周期、状态、**金额快照明细**、费率来源，评审 M1）
6. **FCM device token 表**（评审 m5）
7. 对象存储：Supabase Storage bucket + **signed URL / RLS 按师傅隔离**（评审 M7）

---

## 10. 非功能
- **PII**：客户姓名/电话加密存储 + 接受前隐藏；照片可能含 PII（门锁/家）
- **权限**：师傅只看/操作自己的工单与结算；（评审 M7）照片存储用 signed URL / RLS，防猜 URL 越权
- **通知**：新单/新 A/S 用 **FCM 推送** + 短信兜底
- **离线/弱网**（评审 M5）：照片离线暂存补传；关键操作失败可重试
- **审计**：接受/拒绝/完成/驳回/审批/结算 落审计
- **防双重派单**（评审 m3）：新老派单并行期，确保同一订单不同时进两套流程（切换/去重策略）

---

## 11. 分期

| 阶段 | 内容 |
|---|---|
| **§0 前置** | C1 打包 spike + C2 A/S 数据源决策 |
| **P0** | 翻开关 + 配 env + 录师傅数据（含结构化字段，走 /survey 邀请函）→ **先 test 模式跑通** → 再 production |
| **P1** | Capacitor 脚手架 + 认证（持久化 OTP）+ 我的工单 + 安装接受/拒绝 + FCM |
| **P2** | 安装完成登记（option/照片/结束时间/离线补传/要求-实际对照）+ 总部复核（含驳回）+ 照片查看 |
| **P3** | A/S 管理（建单/派单/接受-拒绝-退回/处理/용역비申报） |
| **P4** | 完整结算（快照冻结 + 费率默认/覆盖 + 导出） |

---

## 12. Open Questions（剩余）
1. 🟡 증상코드 分类表（你之后给）；是否影响派单/费用
2. 🟡 C2：是否导入历史安装记录用于 A/S 追溯
3. 🟡 实际联动等级 > 要求时的计费口径（§6.4C）
4. 🟡 월패드现场金额是否记录数值（§6.4A）
5. 🟡 A/S 是否设 SLA 超时提醒（§7.3）
6. 🟡 师傅会话有效期 / 多设备策略（§6.5）
