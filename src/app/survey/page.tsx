import SurveyClient from "./SurveyClient";

export const metadata = {
  title: "설치 기사 등록 | Aqara",
  description: "설치 기사 정보 등록",
};

export default function SurveyPage() {
  return <SurveyClient />;
}
