# PRD：设置师傅端（Installer App）v0.1（草稿）

> 状态：需求整理中（草稿）。本文件由需求对话逐步沉淀，🟡 标记处为**待你确认**。
> 目标读者：产品/开发。落地前会再经 spec-panel 评审 + workflow 拆解实现计划。

---

## 1. 背景与目标

现在的 backoffice 只给**总部**用。本项目为**安装师傅**新增一个交互端，让师傅能够：

1. 看到派给自己的安装单
2. 接受 / 拒绝派单
3. 上门安装完成后，在对应单上**登记安装结果选项**并**上传结果照片**

同时，这套师傅端会**顺势把已开发完成但一直搁置的「新派单系统」正式接管上线**（见 §3）。

**成功标准**：师傅能全程自助完成「收到单 → 接受 → 完成登记 → 上传照片」，总部据此审核与结算，替代目前「导出 Excel + 人工发短信」的老派单人工流程。

---

## 2. 范围

### In scope（本期）
- 师傅端：登录、我的派工列表、单详情、接受/拒绝、完成登记（选项 + 照片上传）
- 新派单系统正式上线（翻开关、配环境、录入真实师傅数据）
- 总部侧：完成复核、照片查看、**完整结算流程**
- 对象存储（Supabase Storage）用于照片

### Out of scope（明确不做）
- 原生 App（本期倾向 H5/PWA，见 §5）
- 老派单 / `send-assignment-sms` Excel 链路的改造（新派单上线后逐步退役，另行处理）
- 客户侧流程改动（`/i/c` 客户填地址流程沿用现状）

---

## 3. 关键前提：新派单系统上线

代码核查结论：新派单的自动化主线（ERP同步 → 客户填地址 → 匹配师傅 → **总部审核** → 通知师傅 → 接受/拒绝）**已完整实现且测试齐全，只是被开关关闭、且缺师傅端入口**。搁置原因已确认为「做完没启用」。

上线需要（非本 PRD 的功能开发，属配置/数据工作）：
1. 翻开数据库开关：`sync-orders.enabled`、`dispatcher.enabled`、`sms.deliveryMode`(test→production)、`customerInputRequestMode`
2. 配好环境变量：ERP、Solapi、PII 加密 key、cron secret、SMS link base URL
3. **录入真实师傅数据**（服务区域 `serviceAreas`、能力 `capabilities`、`aqaraAppCapability`、电话），否则匹配会走空（`INSTALLER_CANDIDATE_NOT_FOUND`）
4. 保留总部审核环节：AUTO 派单先落 `WAITING_ADMIN_REVIEW`，总部通过后才通知师傅（✅ 已确认保留）

---

## 4. 角色

| 角色 | 说明 |
|---|---|
| 师傅（Installer） | 新增的师傅端使用者；身份 = `installers` 表记录（手机号唯一） |
| 总部（HQ Admin） | 现有 backoffice 用户（`BackofficeUser`，level≥1） |

---

## 5. 平台 🟡

倾向 **H5 / PWA**（复用现有 Next.js、零安装零上架、拍照上传用浏览器 `<input capture>`、更新即时）。原生安卓 App 仅在「必须原生推送/离线」时再考虑，可后续用 WebView 外壳补。
**🟡 待你最终拍板：H5/PWA vs 原生。** （本 PRD 其余部分按 H5/PWA 假设撰写。）

---

## 6. 师傅端功能需求

### 6.1 认证（已定）
- **手机号做账号**（沿用 `installers.phone` 唯一键）
- 首次登录 / 换手机 / 忘记密码：**短信 OTP** 验证
- 验证通过后可**设置密码**，日常用「手机号 + 密码」快速登录
- 只有在 `installers` 表中存在、且状态为启用的号码才能登录
- 复用现有 SMS OTP 能力（`api/auth/sms`），但需为“师傅会话”单独建立（现有 backoffice 用 Supabase，师傅端需独立的师傅 session 机制）🟡（实现细节，评审阶段定）

### 6.2 我的派工列表
- 登录后看到**派给本师傅**的单，按状态分组：
  - 待响应（`WAITING_INSTALLER_RESPONSE`）
  - 已接受待安装（`INSTALLER_ASSIGNED`）
  - 已完成 / 待复核 / 已结算（见 §7）
- 每条显示：安装日期/时段、地址、产品摘要、要求的联动等级
- 🟡 客户姓名/电话在**接受前隐藏**（沿用现有 `/i/i` 的 PII 策略），接受后可见

### 6.3 单详情 + 接受/拒绝
- 复用现有 `respondInstallerAssignment` 逻辑与状态机
- 接受 → `INSTALLER_ASSIGNED`；自动触发「客户已确认」+「happycall 指南」短信（现有）
- 拒绝 → 必须选理由（现有 radio + 其它自填）；触发自动转派下一候选（现有，最多 2 次 AUTO）
- 超时（默认 24h 未响应）→ 自动转派 / 异常（现有 cron）
- ⚠️ 变化点：现在是「一次性 token 链接」，本期改为「登录后在列表里操作」——需要把 token 单单模型接到师傅账号下（一个师傅 = 多个 assignment）

### 6.4 完成登记（本期核心新增）
师傅点「完成」时填写：

**A. 安装结果选项**
| 选项 | 取值 | 是否计费（总部） | 照片要求 |
|---|---|---|---|
| 월패드 연동（壁挂屏联动器） | 是 / 否 | ❌ 0원，仅记录（现场用户直接付师傅） | 无 |
| 联动等级（**递进单选**） | 无 / APP 联动 / 허브 联动 | APP=+5,000원；허브=+20,000원（含APP） | 选 APP 或 허브 → **强制上传 APP 界面照片** |

- 联动等级**递进**：要选허브必须先满足APP（허브 ⊃ APP）
- 直接复用现有枚举 `aqaraAppCapability`（`无 / 门锁+APP / 门锁+APP+网关`），无需新造
- 从**一张 APP 界面照片**即可判断实际是「只联动 APP」还是「联动了网关」，无需按选项分类多张

**B. 照片**
- 至少 1 张、最多 4 张
- 若联动等级 ≥ APP，其中**必须含那张 APP 界面照片**（不满足不允许提交）
- 存 Supabase Storage，关联到该单（新表，见 §8）

**C. 要求 vs 实际 自动对照（增值项）**
- 派单时系统已从 ERP 推断出「本单要求的联动等级」(`required_aqara_app_capability`)
- 师傅填的是「实际联动等级」
- 实际 < 要求（如客户买网关套餐、师傅只装了 APP）→ 系统自动标记异常，供总部复核

---

## 7. 总部侧（backoffice）

### 7.1 派单审核（保留，已定）
沿用现有 `WAITING_ADMIN_REVIEW` → 总部通过 → 通知师傅。

### 7.2 完成复核 🟡（建议加）
建议：师傅提交完成（选项 + 照片）后进入**「待复核」**，总部核对照片/选项无误 → 确认 → 才算最终完成并进结算。
理由：联动等级直接计费、照片防虚报，加一道复核更稳。**🟡 待确认：要不要这道复核？还是师傅提交即完成？**

### 7.3 照片查看
总部在单详情能查看师傅上传的照片（含那张 APP 界面证明照）。

### 7.4 结算（完整流程，已定要做完整版）
**费用规则（已定）**：
- 每单结算金额 = 联动等级对应费用：APP=5,000 / 허브=20,000 / 无=0
- 월패드 연동不计入（0원，现场结清，仅记录）

**完整结算流程包含**（🟡 以下细节待你补）：
- 结算周期：🟡 按月？按批次？自定义区间？
- 是否含**基础安装费 / 출장비(出差费)**？（老派单 Excel 里有 `00010 출장비`）🟡
- 汇总视图：按师傅 / 按周期 汇总应付金额
- 结算状态机：待结算 → 已结算 → 已发放 🟡（状态定义待定）
- 谁能操作结算：🟡（哪个 level 的总部用户）
- 导出格式：🟡（Excel？对接财务？）
- 师傅端要不要看到自己的结算明细？🟡

---

## 8. 数据模型改动（初步，评审阶段细化）

**复用/扩展现有**：
- `InstallationOrder` / `InstallationInstallerAssignmentAttempt` / `Installer` / `InstallationOrderStatusEvent`
- 状态机 `status.ts` 已允许 `INSTALLER_ASSIGNED → COMPLETED`；新增以 `actorType:"INSTALLER"` 触发的完成动作
- `aqaraAppCapability` 复用为「实际联动等级」

**新增（系统当前完全没有）**：
1. **师傅账号/会话**：师傅密码（hash）、session 机制（`installers` 表扩展或新表）
2. **完成凭证表** `InstallationCompletionEvidence`（或类似）：FK 到单/assignment，记录 월패드연동、实际联动等级、照片 URL 列表、提交时间、提交人
3. **对象存储**：Supabase Storage bucket + 上传路径规范 + 访问权限（师傅只能传/看自己单的，总部可看全部）
4. **结算表**：结算记录、周期、状态、金额明细

---

## 9. 非功能需求
- **PII**：客户姓名/电话沿用现有加密存储 + 接受前隐藏策略
- **权限**：师傅只能看/操作派给自己的单；照片存储做好按师傅隔离
- **通知**：新单通知沿用短信（已有 `installer_assignment_request`）；🟡 是否加 PWA 推送/站内提醒？
- **审计**：完成/复核/结算动作都要落 `InstallationOrderStatusEvent`

---

## 10. 建议分期

| 阶段 | 内容 |
|---|---|
| **P0 上线地基** | 翻开关 + 配环境 + 录师傅数据，让新派单能真实跑（无新功能开发） |
| **P1 师傅端 MVP** | 认证 + 我的派工 + 接受/拒绝（复用现有逻辑，接到账号下） |
| **P2 完成登记** | 完成选项 + 照片上传 + 对象存储 + 要求/实际对照 + 总部照片查看/复核 |
| **P3 结算** | 完整结算流程 |

🟡 待确认：是否认可这个分期与顺序？

---

## 11. 待确认清单（Open Questions）

1. 🟡 平台最终定：H5/PWA vs 原生安卓（§5）
2. 🟡 完成后是否加「总部复核」一道（§7.2）
3. 🟡 结算细节：周期、是否含出差费/基础安装费、状态定义、操作权限、导出格式、师傅可见性（§7.4）
4. 🟡 师傅端要不要加推送/站内提醒，还是只靠短信（§9）
5. 🟡 分期顺序是否认可（§10）
